package restmqscala

import org.specs._
import org.specs.mock.Mockito
//import org.mockito.Matchers._ 
import unfiltered.request._

/*class RestMQPlanSpec extends Specification with Mockito {
  val m = mock[RestMQBroker]
  val QN = "testqueue"

  "Plan" should {
    "add parameter 'value' to the queue specified by it's path" in {
      m.get(QN) returns Some("hello")

      m.get(QN) must_== Some("fdfsdf")

//      there was one(m).get(QN)
    }
  }
}*/